Django>=5.2,<6
django-htmx>=1.21
celery[redis]>=5.4
redis>=5.0
django-celery-beat>=2.7
django-celery-results>=2.5
requests>=2.32
psycopg[binary]>=3.2
python-decouple>=3.8
gunicorn>=23.0
whitenoise>=6.7
pyTelegramBotAPI>=4.20

# Telemetry (v2). FastF1 pulls in pandas + numpy; only the worker really
# needs it at runtime, but it's installed everywhere so management commands
# and tests work uniformly.
fastf1>=3.4

# dev / tooling
black>=24.10
isort>=5.13
pre-commit>=4.0
pytest>=8.3
pytest-django>=4.9
responses>=0.25
